# Nothing here may enter a build layer. A secret baked into an image is
# permanent and shippable, which is worse than one on disk.
sessions/
out/
node_modules/
*.env
.env*
*credential*
*password*
*secret*
*.pem
*.key
cookies.json
storage-state.json
